# SIM 5 — glue: cascade → superfan → cadence → treasury

Spec: [SPEC.md](SPEC.md). sim4 is the play matrix; this is the pipe the
three older worlds were missing.

```
python3 emp2/cadence_measure.py   # UNMEASURED without ≥200 rows — that's valid
python3 sim5/glue.py             # gates G1–G3, fig20
```

Cadence from emp2 if measured; otherwise the open range 0.38–1.25–6.31 and
check $6.89. Twitch k=12 is not an input.

Headline (k=4, check $6.89, take 0.80, frozen sim2 table seed=42):

| B=5 | L listeners | σ* for $100/mo | vs σ band 0.6–1.7% |
|---|---|---|---|
| hubs | 22,550.5 | **0.242%** | inside / below |
| random | 5 | ≫ 1 | above — does not fund |

Worst open corner (k=0.38, $3.10) on hubs: σ* ≈ 5.65% — loses to the 1.7%
ceiling. The joint GTM+econ claim is a map until emp2 fills.
